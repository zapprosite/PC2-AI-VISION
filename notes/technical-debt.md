# Technical Debt

- `claude-code-router` em `0.0.0.0` sem auth
- OpenCode Desktop tem histórico de modelos externos
- `gh` sem login
- configs sensíveis espalhadas em `~/.config` e `~/.local/share`
- `swap` em `0B`
- state repo deve evitar configs brutas

# Exposure Policy

Bindings locais preferidos:

- `127.0.0.1` para cockpit, túneis e serviços auxiliares

Bindings que exigem justificativa e policy:

- `0.0.0.0`

No estado atual, `claude-code-router` é tratado como exceção de risco alto. Não normalizar esse bind como padrão do PC2.
